# 1. Start teleop with recording (Teleop_with_record.py), refer to steps in remote_ctrl_README.md, save raw data(npz files).

# 2. Convert raw data to lerobot dataset with lerobot/examples/port_datasets/npz_format.py.

# 3. Upload lerobot dataset to server.
